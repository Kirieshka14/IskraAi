import Link from "next/link"; import { Heart, MessageCircle } from "lucide-react"; import type { Bot } from "@/lib/types"; import { genreLabels } from "@/lib/mock-data";
const colors=["bg-[#d9a07d]","bg-[#789587]","bg-[#bba66e]","bg-[#a87d79]","bg-[#758ca1]","bg-[#9b8a73]"];
export function BotCard({bot,index=0}:{bot:Bot;index?:number}){return <article className="group overflow-hidden rounded-2xl border border-stone-200 bg-white transition sm:hover:-translate-y-1 hover:shadow-soft"><div className={`relative grid aspect-[4/3] place-items-center ${colors[index%colors.length]}`}><span className="text-5xl font-black text-white/85">{bot.avatar}</span><span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1 text-xs font-semibold text-stone-700">{genreLabels[bot.genre]}</span></div><div className="p-4"><div className="mb-2 flex items-start justify-between gap-3"><div><h3 className="text-lg font-bold">{bot.name}</h3><p className="text-xs text-stone-500">автор @{bot.author}</p></div></div><p className="line-clamp-3 min-h-[60px] text-sm leading-5 text-stone-600">{bot.description}</p><div className="mt-4 flex items-center justify-between border-t border-stone-100 pt-3"><div className="flex gap-3 text-xs text-stone-500"><span className="flex items-center gap-1"><Heart size={14}/>{bot.likes.toLocaleString("ru-RU")}</span><span className="flex items-center gap-1"><MessageCircle size={14}/>{Math.round(bot.conversations/1000)} тыс.</span></div><Link href={`/chat/${bot.id}`} className="inline-flex min-h-11 items-center text-sm font-bold text-ember hover:underline">Начать →</Link></div></div></article>}
