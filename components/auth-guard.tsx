"use client";import{useEffect,useState}from"react";import{usePathname,useRouter}from"next/navigation";import{apiRequest}from"@/lib/api";const PUBLIC=["/auth","/register","/auth/callback"];
export function AuthGuard({children}:{children:React.ReactNode}){const path=usePathname(),router=useRouter(),[ready,setReady]=useState(PUBLIC.some(x=>path.startsWith(x)));useEffect(()=>{if(PUBLIC.some(x=>path.startsWith(x))){setReady(true);return}apiRequest("/api/points").then(()=>setReady(true)).catch(()=>router.replace(`/auth?next=${encodeURIComponent(path)}`))},[path,router]);return ready?<>{children}</>:<main className="grid min-h-screen place-items-center"><p>Проверяем сессию…</p></main>}
